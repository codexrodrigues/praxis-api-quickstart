# Example env file for local dev (profile=dev)
# Use: export $(cat .env.dev.example | xargs) or a .env loader

# Neon JDBC URL (omit channel_binding; ensure sslmode=require)
# Derived from DSN: postgresql://<user>:<pass>@<host>/<db>?sslmode=require
SPRING_DATASOURCE_URL=jdbc:postgresql://ep-blue-butterfly-ad8tnape-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require
SPRING_DATASOURCE_USERNAME=neondb_owner
SPRING_DATASOURCE_PASSWORD=REPLACE_WITH_REAL_PASSWORD

# Optional tuning
DB_POOL_SIZE=5
SPRING_PROFILES_ACTIVE=dev

