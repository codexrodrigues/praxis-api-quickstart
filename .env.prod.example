# Example env file for production (Render, profile=prod)

# Render: add the vars below in the Dashboard → Environment → Environment Variables

# Neon JDBC URL (sslmode required; channel_binding is not used by JDBC driver)
SPRING_DATASOURCE_URL=jdbc:postgresql://ep-blue-butterfly-ad8tnape-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require
SPRING_DATASOURCE_USERNAME=neondb_owner
SPRING_DATASOURCE_PASSWORD=REPLACE_WITH_RENDER_SECRET

# Pool
DB_POOL_SIZE=10

# Activate production profile
SPRING_PROFILES_ACTIVE=prod

